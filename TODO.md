1. [x] 评审skill放到上级目录，和卡片目录脱离
2. [x] skill内部不包括具体卡片信息，删除和具体卡片相关的内容
3. [x] 评测维度不够，要包含卡片业务逻辑处理，if else条件是否完备，xss越权之类的评测。需要参考市面上常见的code-review工具或者git仓库。评测指标可以放到reference文件夹下作为知识库来源
4. [x] 卡片会有一些特殊要求，比如cubecard不允许有for嵌套之类的，是否有加入到校验中
5. [x] eslint校验不够，需要增加规则。能否加上对卡片的jsdoc校验，确保所有的方法和模块都有注释，注释内容正确。
6. [x] 卡片会包含阿里的F2图表，需要增加图表的校验。
7. [x] 卡片会有一些客户端自定义的规则和element，还用项目封装的自定义组件，如何创建配置项添加这些，并在评测过程中自动生成校验规则和测试代码。
8. [x] 一些业务规则，eslint规则，jsdoc之类的都需要有一个配置项可以登记，实现语义化的添加，并支持配置是强制报错还是警告，然后生成对应的校验规则。
9. [x] 通过json格式，可以做到哪些校验，说明一下
10. [x] 要求有文件可以配置需要校验的卡片id，添加卡片的测试案例，然后跑这些卡片的校验流程，生成单元测试代码和e2e代码，输出反馈报告。这样一个端到端的评测循环。并提供cli命令可以手动执行这个流程。
11. [x] 卡片会有 `const mpaasApi = requireModule('mpaas_jsapi')`来引用客户端的api组件，如何校验他们是否使用正确
12. [x] 一些数据边界校验也要安排上，比如数组长度是否超过最大限制，对象属性是否超过最大限制之类的。
13. [x] 有些卡片会涉及到一些复杂的交易流程，需要调用多个接口，但是卡片的接口之间不能出现并行调用。如何设计校验规则，确保交易流程的正确性，并使用mermaid图绘制交易流程的序列图和时序图之类的用于展示。
14. [x] 所有的评测设计代码里的方法和模块加上注释，说明一下他们的作用和参数。
15. [x] 生产的卡片会外部引入common公共方法，env环境变量（sit，uat，prod），如何校验他们是否使用正确
16. [x] css报错，是否能配置eslint进行检验，确保css代码符合规范
17. [x] 评审维度（E2E 层）感觉不够丰富，能否增加一些校验规则，比如卡片的性能是否符合要求，是否符合用户需求之类的。然后每个维度的评审代码也是单独拆出来，方便维护。
18. [x] 动态测试的不同维度是不是也可以单独拆出来，比如性能测试，功能测试，安全测试之类的。
19. [x] ACT Cube 卡片代码评审知识库（来源）最好是下载下来，因为到时候skill要部署到内网，这些参考不了的。
20. [x] 新增的图片放assets下，图片要校验不要大于400k，然后格式是png
21. [x] readme skill review的md要更新下
22. [x] 最后要出的一个测试review是放在哪里，是md加上report json吗
23. [x] 卡片的配置能否放在skill最外层加一个yaml文件
24. [x] 单元测试，规则测试，eslint测试，e2e测试能否按照文件夹归类下，现在都散在同一级不方便看
25. [x] 能不能在skill同级建立一个test文件夹，再建立按照卡片id分类的子文件夹，用来放置属于各个卡片自己的测试案例md，生成属于各个卡片特有的测试js，然后生成的测试报告也放在这里。
26. [x] rules文件夹，现在有很多规则了，能不能也分文件夹放，按照不同的维度规则，后续也可以方便按维度扩展。并且需要一个维度说明md
27. [x] 加一个功能，每张卡片都会有自己的测试案例md，需要通过测试案例生成更严谨全面的单元测试和端到端测试。这一步需要借助agent api。你提供输入需要的文件列表，修改规则，我提供测试案例excel表，要求生成测试案例md，属于这张卡片的测试代码，并完成测试。需要有配置项填写agent的api provider key model之类的参数，然后写调用方法。实现通过外部agent生成规范的测试案例的过程
28. [x] 使用定时器时，销毁卡片的生命周期一定要关闭，这个加一下规则
29. [x] 卡片配置yaml有了，但是config.js没了，还是要的，config.js的配置负责填写卡片相关的，yaml还要承载其他配置的。而且yaml要放到skill的根目录下作为整个skill的统一配置登记文件。
30. [x] 确认eslint规则包含vue css和script，确认eslint载入cube自定义规则，确认eslint规则都作用于卡片。然后这些规则都放到同一个目录下，命名eslint
31. [x] 确认validate规则包含reference里的各个维度，然后validate相关的放一个文件夹下
32. [x] 确认单元测试和端到端测试包含reference里的各个维度，然后这两个相关的放一个文件夹下
33. [x] eslint，validate，unit e2e这几个校验各自负责的部分是啥，readme和skillmd里都写清楚
34. [x] agent相关的配置，在yaml中也需要补充一份，skill加载时优先获取yaml中的配置，再读取config配置
35. [x] 项目里 eslint-plugin-prettier 跟 eslint 9 不兼容，最后再检查下package.json里的依赖和脚本，是否有遗漏的或者不兼容的。
36. [x] report.json和REVIEWmd是模板还是生成的报告，是否要挪到test/<card>/里
37. [x] skill里各个实现方法都加一下注释，然后删除和实际卡片相关的注释信息